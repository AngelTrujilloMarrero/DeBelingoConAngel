# ========================================
# FRONTEND Environment Variables (Vite)
# ========================================
# Estas variables se exponen al cliente y deben tener el prefijo VITE_

# Vercel Backend API URL
# URL del backend de Vercel que maneja las subidas de imágenes
VITE_VERCEL_API_URL=https://de-belingo-con-angel.vercel.app

# Firebase Configuration
# Obtén estas credenciales desde Firebase Console: Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=tu_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=tu-proyecto.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://tu-proyecto-default-rtdb.europe-west1.firebasedatabase.app
VITE_FIREBASE_PROJECT_ID=tu-proyecto-id
VITE_FIREBASE_STORAGE_BUCKET=tu-proyecto.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=tu_sender_id
VITE_FIREBASE_APP_ID=tu_app_id
VITE_FIREBASE_MEASUREMENT_ID=tu_measurement_id

# ========================================
# BACKEND Environment Variables (Vercel)
# ========================================
# Estas variables NO deben tener el prefijo VITE_
# Solo están disponibles en las serverless functions de Vercel
# Configúralas en: Vercel Dashboard > Settings > Environment Variables

# IMPORTANTE: NO agregues estas variables aquí si vas a commitear este archivo
# Solo están documentadas para referencia. Los valores reales deben estar en Vercel.

# Image Upload Services (SOLO para Vercel Backend)
# IMGBB_API_KEY=tu_imgbb_api_key
# IMGUR_CLIENT_IDS=client_id_1,client_id_2,client_id_3

# ========================================
# NOTAS IMPORTANTES
# ========================================
#
# 1. FRONTEND (VITE_* variables):
#    - Se incluyen en el bundle del cliente
#    - Firebase API keys son seguras aquí gracias a Firebase Security Rules
#    - Configurar en Vercel con el prefijo VITE_
#
# 2. BACKEND (sin VITE_ prefix):
#    - Solo accesibles desde funciones serverless de Vercel
#    - NUNCA se exponen al navegador
#    - Configurar SOLO en Vercel Dashboard, NO en .env files
#
# 3. SEGURIDAD:
#    - .env está en .gitignore - nunca se commitea
#    - .env.example solo muestra la estructura - sin valores reales
#    - Valores reales SOLO en Vercel Dashboard o .env local
#
# 4. DEPLOYMENT:
#    - Local: copia .env.example a .env y completa los valores
#    - Vercel: configura variables en Dashboard > Environment Variables
#    - Ver VERCEL_BACKEND_SETUP.md para instrucciones detalladas